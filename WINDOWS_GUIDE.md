# 🐾 Pet Food Shop - คู่มือการใช้งานสำหรับ Windows

## 📋 สิ่งที่คุณต้องมี

- Windows 10/11
- RAM อย่างน้อย 4GB
- พื้นที่ว่าง 10GB
- Internet connection

## 🚀 วิธีติดตั้งแบบง่าย

### ขั้นตอนที่ 1: ติดตั้ง Docker Desktop

1. **ดาวน์โหลด Docker Desktop**
   - ไปที่: https://www.docker.com/products/docker-desktop
   - คลิก "Download for Windows"
   - รอให้ดาวน์โหลดเสร็จ

2. **ติดตั้ง Docker Desktop**
   - เปิดไฟล์ที่ดาวน์โหลด
   - ทำตามขั้นตอนการติดตั้ง
   - **รีสตาร์ทคอมพิวเตอร์** หลังติดตั้งเสร็จ

3. **เปิด Docker Desktop**
   - หาโปรแกรม "Docker Desktop" ในเมนู Start
   - เปิดโปรแกรมและรอให้โหลดเสร็จ
   - เมื่อเห็นสีเขียวแสดงว่าพร้อมใช้งาน

### ขั้นตอนที่ 2: ติดตั้งร้านค้า

**วิธีที่ 1: ใช้ไฟล์ .bat (แนะนำ)**
```
1. ดับเบิลคลิก "install-windows.bat"
2. รอให้ติดตั้งเสร็จ (ประมาณ 5-10 นาที)
3. เว็บไซต์จะเปิดขึ้นมาเองที่ http://localhost:8000
```

**วิธีที่ 2: ใช้ PowerShell (สำหรับผู้ที่ต้องการความแม่นยำ)**
```
1. คลิกขวาที่ "install-windows.ps1"
2. เลือก "Run with PowerShell"
3. หรือ เลือก "Open with Windows PowerShell ISE"
4. กด F5 เพื่อรัน
```

## 🌐 การเข้าใช้งาน

หลังติดตั้งเสร็จ คุณสามารถเข้าใช้ได้ที่:

- **ร้านค้าหลัก**: http://localhost:8000
- **หน้าแอดมิน**: http://localhost:8000/wp-admin
- **จัดการฐานข้อมูล**: http://localhost:8080
- **ทดสอบอีเมล**: http://localhost:8025

## 🎮 การจัดการร้านค้า

### เริ่มร้านค้า
- ดับเบิลคลิก `start.bat`
- หรือรันคำสั่ง: `docker-compose up -d`

### หยุดร้านค้า
- ดับเบิลคลิก `stop.bat`
- หรือรันคำสั่ง: `docker-compose down`

### รีสตาร์ทร้านค้า
- ดับเบิลคลิก `restart.bat`
- หรือรันคำสั่ง: `docker-compose restart`

## 🛒 สินค้าตัวอย่างที่ได้

ระบบจะสร้างสินค้าตัวอย่างให้ 5 รายการ:
- อาหารสุนัขระดับพรีเมียม (450 บาท)
- อาหารแมวดีลักซ์ (380 บาท)
- ลูกบอลของเล่น (280 บาท)
- เตียงสัตว์เลี้ยงนุ่มสบาย (890 บาท)
- แชมพูสัตว์เลี้ยงอ่อนโยน (320 บาท)

## ⚙️ การปรับแต่ง

### เปลี่ยนโดเมน
1. แก้ไขไฟล์ `.env`
2. เปลี่ยน `APP_URL=http://localhost:8000` เป็นโดเมนของคุณ
3. รีสตาร์ทร้านค้า

### เปลี่ยนพอร์ต
1. แก้ไขไฟล์ `.env`
2. เปลี่ยน `WORDPRESS_PORT=8000` เป็นพอร์ตที่ต้องการ
3. รีสตาร์ทร้านค้า

### เพิ่มสินค้า
1. เข้าหน้าแอดมิน: http://localhost:8000/wp-admin
2. ไปที่ Products > Add New
3. เพิ่มข้อมูลสินค้าและรูปภาพ

## 🔧 การแก้ปัญหา

### ปัญหา: Docker Desktop ไม่เริ่มทำงาน
**วิธีแก้:**
1. เปิด Task Manager (Ctrl+Shift+Esc)
2. หา "Docker Desktop" และปิดโปรเซส
3. เปิด Docker Desktop ใหม่
4. รอให้สถานะเป็นสีเขียว

### ปัญหา: Port ถูกใช้งานแล้ว
**วิธีแก้:**
1. แก้ไขไฟล์ `.env`
2. เปลี่ยนพอร์ตเป็นเลขอื่น เช่น 8001, 8002
3. รีสตาร์ทร้านค้า

### ปัญหา: เว็บไซต์ไม่เปิด
**วิธีแก้:**
1. ตรวจสอบ Docker Desktop ว่าเปิดอยู่
2. รันคำสั่ง `docker-compose ps` เพื่อดูสถานะ
3. รอสักครู่แล้วลองใหม่

## 📂 การคัดลอกไปเซิร์ฟเวอร์อื่น

### วิธีการ 1: คัดลอกโฟลเดอร์
1. คัดลอกโฟลเดอร์ทั้งหมดไปเครื่องใหม่
2. ติดตั้ง Docker Desktop บนเครื่องใหม่
3. รัน `install-windows.bat` บนเครื่องใหม่

### วิธีการ 2: สร้างแพ็คเกจ
1. รัน `create-web-template.sh` (ใน Git Bash หรือ WSL)
2. ได้โฟลเดอร์ `pet-food-shop-template`
3. คัดลอกโฟลเดอร์นี้ไปเครื่องใหม่
4. รัน `install.sh` หรือ `install-windows.bat`

## 💡 เทิปการใช้งาน

### ประสิทธิภาพดีขึ้น
- ปิดโปรแกรมอื่นๆ ขณะติดตั้ง
- ใช้ SSD จะเร็วกว่า HDD
- เพิ่ม RAM ให้ Docker Desktop ในการตั้งค่า

### ความปลอดภัย
- เปลี่ยนรหัสผ่านฐานข้อมูลในไฟล์ `.env`
- ตั้งรหัสผ่านแอดมินที่แข็งแรง
- ใช้ HTTPS สำหรับการใช้งานจริง

### การสำรองข้อมูล
- สำรองโฟลเดอร์ `wp-content` เป็นประจำ
- ใช้ phpMyAdmin เพื่อส่งออกฐานข้อมูล
- เก็บไฟล์ `.env` ไว้สำรอง

## 🆘 ต้องการความช่วยเหลือ

หากมีปัญหา:
1. ตรวจสอบ Docker Desktop ให้เปิดอยู่
2. ดูไฟล์ log: `docker-compose logs -f`
3. รีสตาร์ทระบบทั้งหมด: `docker-compose down && docker-compose up -d`
4. ลองติดตั้งใหม่หากจำเป็น

## 🎯 เหมาะสำหรับ

- ร้านขายอาหารสัตว์เลี้ยง
- ร้านอุปกรณ์สัตว์เลี้ยง
- คลินิกสัตวแพทย์
- ธุรกิจเกี่ยวกับสัตว์เลี้ยง

ขอให้สนุกกับการใช้งานร้านค้าสัตว์เลี้ยงของคุณ! 🐾